{
  "name": "wspp",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "keywords": [],
  "license": "ISC",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev:build": "tsc --watch",
    "dev:start": "node --watch -r dotenv/config build/server.js",
    "dev:watch": "npm run dev:build & npm run dev:start",
    "dev": "tsc && node -r dotenv/config build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pug": "^3.0.2",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.3.1",
    "@types/ws": "^8.5.5",
    "dotenv": "^16.3.1",
    "typescript": "^5.1.3"
  }
}
